import Pokemons from "../Pokemons/Pokemons"

const PokemonList = ( ) => { //props es un objeto, hace que podamos reutilizar el codigo

    

/*     console.log(props.name); //entrar a la propiedad
 */    return(
        <div>
            <Pokemons type="fire" />
        </div>
    )

}

export default PokemonList